import { loadInputDataFromDay } from "../common/input.js";

function part1(inputs: string[]) {
}

function part2(inputs: string[]) {
}

function main() {
    const inputs = loadInputDataFromDay(10);
    console.log(part1(inputs));
    console.log(part2(inputs));
}

main();

